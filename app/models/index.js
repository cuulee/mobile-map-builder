import images from './Images'
import map from './Map'
import metadata from './Metadata'
import tiles from './Tiles'

const models = {
  Images: images,
  Map: map,
  Metadata: metadata,
  Tiles: tiles
}

export const { Images, Map, Metadata, Tiles } = models
export default models
